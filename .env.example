# Developer Console(dev.worksmobile.com) > API > ClientApp 에서 발급
WORKS_CLIENT_ID=
WORKS_CLIENT_SECRET=

# 일반용 기본값. 조직이 공공용(gov) 네이버웍스로 바뀌면 아래 두 값만 교체한다.
WORKS_API_BASE=https://www.worksapis.com
WORKS_AUTH_BASE=https://auth.worksmobile.com
